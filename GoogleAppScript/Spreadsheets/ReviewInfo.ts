export interface ReviewIdentifier {
    responseId: string
}

export enum Column {
    Number,
    Version,
    Faction,
    Name,
    Date,
    Reviewer,
    Deck,
    Count,
    Rating,
    Release,
    Reason,
    Additional,
    ResponseId
}